---
import type { MetaLine } from '../lib/captions';

interface Props {
  label: string;
  title: string;
  dek: string;
  meta: MetaLine;
}

const { label, title, dek, meta } = Astro.props;
---
<header class="head">
  <p class="label meta">{label}</p>
  <h1 class="title display">{title}</h1>
  {dek && <p class="dek">{dek}</p>}
  <p class="line meta">{meta.published && <Fragment>Originally published in {meta.published.href ? <a href={meta.published.href} rel="noopener">{meta.published.label}</a> : <span>{meta.published.label}</span>}{meta.items.length > 0 && ' · '}</Fragment>}{meta.items.join(' · ')}</p>
</header>

<style>
  .label { font-size: 11px; letter-spacing: 0.06em; color: var(--muted); margin-bottom: 14px; }
  .title {
    font-size: 38px;
    line-height: 1.1;
    font-weight: 300;
    letter-spacing: -0.01em;
    text-wrap: balance;
    margin-bottom: 14px;
  }
  .dek { font-style: italic; font-size: 19px; line-height: 1.4; color: var(--paper-text); margin-bottom: 20px; }
  .line { font-size: 12px; color: var(--muted); padding-bottom: 20px; border-bottom: 1px solid var(--line); }
  @media (max-width: 639px) {
    .title { font-size: 30px; line-height: 1.12; }
    .dek { font-size: 18px; line-height: 1.4; }
  }
</style>
