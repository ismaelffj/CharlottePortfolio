---
interface Props {
  html: string;
}

const { html } = Astro.props;
---
<div class="prose" set:html={html} />

<style>
  .prose { font-size: 18px; line-height: 1.6; }
  .prose :global(p) { margin: 0 0 22px; }
  .prose :global(h2) { font-family: var(--font-display); font-size: 24px; line-height: 1.2; font-weight: 400; margin: 32px 0 12px; }
  .prose :global(h3) { font-family: var(--font-display); font-size: 20px; line-height: 1.25; font-weight: 400; margin: 28px 0 10px; }
  .prose :global(blockquote) { margin: 0 0 22px; padding-left: 18px; border-left: 1px solid var(--line); font-style: italic; }
  .prose :global(ul), .prose :global(ol) { margin: 0 0 22px; padding-left: 22px; }
  .prose :global(li + li) { margin-top: 6px; }
  .prose :global(hr) { border: 0; border-top: 1px solid var(--line); margin: 32px 0; }
</style>
