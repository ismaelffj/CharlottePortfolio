---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import { smartQuotes } from '../lib/text';

interface Props {
  statement: string;
  portrait: ImageMetadata | null;
  alt: string;
}

const { statement, portrait, alt } = Astro.props;
---
<section class="wrap">
  <div class="hero">
    <h1 class="statement display">{smartQuotes(statement)}</h1>
    {portrait && (
      <div class="portrait">
        <Image src={portrait} alt={alt} width={400} densities={[1, 2]} priority />
      </div>
    )}
  </div>
</section>

<style>
  .hero {
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: 80px;
    align-items: center;
    margin: 64px 0 96px;
  }
  .statement {
    font-size: 56px;
    line-height: 1.08;
    font-weight: 340;
    letter-spacing: 0;
    text-wrap: balance;
    hanging-punctuation: first;
    max-width: 640px;
  }
  .portrait { position: relative; aspect-ratio: 4 / 5; overflow: hidden; }
  .portrait :global(img) { width: 100%; height: 100%; object-fit: cover; }
  .portrait::after {
    content: '';
    position: absolute;
    inset: 0;
    box-shadow: inset 0 0 0 1px var(--keyline);
    pointer-events: none;
  }
  @media (max-width: 1023px) {
    .hero { grid-template-columns: 1fr 300px; gap: 48px; }
    .statement { font-size: 44px; }
  }
  @media (max-width: 899px) {
    .hero { grid-template-columns: 1fr; gap: 32px; margin: 48px 0 64px; }
    .portrait { max-width: 400px; }
  }
  @media (max-width: 639px) {
    .hero { gap: 28px; margin: 44px 0 56px; }
    .statement { font-size: 36px; line-height: 1.1; }
    .portrait { max-width: none; }
  }
</style>
