---
interface Props {
  label: string;
}

const { label } = Astro.props;
---
<div class="wrap">
  <section class="row">
    <h2 class="label">{label}</h2>
    <div class="text">
      <slot />
    </div>
  </section>
</div>

<style>
  .row {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: 32px;
    padding: 40px 0;
    border-top: 1px solid var(--line);
  }
  .label { font-style: italic; font-size: 17px; font-weight: 400; color: var(--text); padding-top: 4px; }
  .text { max-width: 640px; font-size: 21px; line-height: 1.55; }
  :global(body.wine) .text { font-weight: 450; }
  .text :global(p + p) { margin-top: 12px; }
  @media (max-width: 899px) {
    .row { grid-template-columns: 1fr; gap: 8px; padding: 28px 0; }
    .text { font-size: 18px; line-height: 1.5; }
  }
</style>
